{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities available to the main window in Forge Workspace.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
